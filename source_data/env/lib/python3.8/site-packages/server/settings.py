"""
Repository for all constant, editable settings for the writing of data.

These should be set as environment variables on each system:
OUTPUT_BASE: the path into which all HDF5 output files should be written. Further directories will be made inside here


"""
import os
from common.settings import *


# # --- Settings for reading/writing HDF5 files  --- # #
OUTPUT_BASE = os.environ.get('HDF5_OUTPUT', r'D:\NeurohubSystems\DataNet\temp-data\write_tests')
#: Duration of each chunk in the HDF5, chunk size determined on this and sampling frequency. If None, no chunking
CHUNK_DURATION_SECONDS = None     # in seconds
#: Compression method to use in the HDF5 files. If None, no compression
COMPRESSION = None


# Accelerometer parsing settings
#: Name of csv file which contains the raw RCS accelerometer data
RCS_ACCEL_FILENAME = 'AccelTimeDomain.csv'
#: Default sampling frequency of the RCS accelerometer data, used for interpolation when no data is available
DEFAULT_CHEST_SF = 64   # Hz
POSSIBLE_CHEST_SFS = [64, 32, 16, 8, 4]  # Hz
DEFAULT_WATCH_SF = 50
POSSIBLE_WATCH_SFS = [50]
#: Method to use for upsampling when resampling accelerometer data. passed to pd.DataFrame.interpolate
WATCH_RESAMPLE_METHOD = 'cubicspline'

#: Name of JSON file which stores all the raw (OpenMind output) settings data
SETTINGS_FILENAME = "BaseSettings.json"

RCS_EVENTS_FILENAME = 'EventLog.csv'

NEURAL_TS_FILENAME = 'NeuralTimeDomain.csv'
